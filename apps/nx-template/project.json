{
  "name": "nx-template",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/nx-template/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/nx-template",
        "main": "apps/nx-template/src/main.ts",
        "tsConfig": "apps/nx-template/tsconfig.app.json",
        "assets": ["apps/nx-template/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/nx-template/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "nx-template:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "nx-template:build:development"
        },
        "production": {
          "buildTarget": "nx-template:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/nx-template/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/nx-template/jest.config.ts"
      }
    }
  },
  "tags": []
}
