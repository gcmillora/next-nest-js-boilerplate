import { Input } from '@nx-template/web-app/ui-components';

export default async function Index() {
  /*
   * Replace the elements below with your own.
   *
   * Note: The corresponding styles are in the ./index.css file.
   */
  return (
    <div>
      <Input />
    </div>
  );
}
